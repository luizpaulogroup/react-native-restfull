import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    safeAreaView: {
        flex: 1,
        backgroundColor: '#FFF',
    },
    buttonCreate: {
        position: 'absolute',
        right: 20,
        bottom: 20,
    },
});

export default styles;